apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base

secretGenerator:
  - name: app-secrets
    envs:
      - ../../secrets/app.env

generatorOptions:
  disableNameSuffixHash: true
